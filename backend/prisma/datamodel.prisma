enum Permission {
  SUPERADMIN
  ADMIN
  USER
  ARTIST
  PERMISSIONUPDATE
}

type User {
  id: ID! @id
  email: String! @unique
  password: String!
  createdAt: DateTime!
  updatedAt: DateTime!
  emailConfirmToken: Float!
  emailConfirmed: Boolean
  permissions: [Permission]! @scalarList(strategy: RELATION)
}
