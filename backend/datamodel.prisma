enum Permission {
  SUPERADMIN
  ADMIN
  USER
  ARTIST
  ITEMCREATE
  ITEMUPDATE
  ITEMDELETE
  PERMISSIONUPDATE
}

type User {
  id: ID! @id
  email: String! @unique
  password: String!
  token: String
  permissions: [Permission]! @scalarList(strategy: RELATION)
}
